import { RoleIdToTitlePipe } from './role-id-to-title.pipe';

describe('RoleIdToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleIdToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
