import { JobIdToTitlePipe } from './job-id-to-title.pipe';

describe('JobIdToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new JobIdToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
