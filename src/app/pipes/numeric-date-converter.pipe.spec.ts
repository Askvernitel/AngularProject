import { NumericDateConverterPipe } from './numeric-date-converter.pipe';

describe('NumericDateConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new NumericDateConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
